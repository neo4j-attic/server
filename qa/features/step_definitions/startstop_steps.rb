And /^Neo4j Home based on system property "([^"]*)"$/ do |home|
  pending # express the regexp above with the code you wish you had
end

And /^Neo4j Server installed in Neo4j Home$/ do
  pending # express the regexp above with the code you wish you had
end

When /^I start Neo4j Server$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^"([^"]*)" should provide the Neo4j REST interface$/ do |uri|
  pending # express the regexp above with the code you wish you had
end
